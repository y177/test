{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": ["Read", "Edit", "Write", "Bash", "WebFetch", "Skill"]
  },
  "env": {
    "NOTION_API_KEY": "${NOTION_API_KEY}",
    "NOTION_DATABASE_ID": "${NOTION_DATABASE_ID}"
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '🚀 Notion Manager 프로젝트에 오신 것을 환영합니다!'"
          },
          {
            "type": "command",
            "command": "npm install --silent"
          }
        ]
      }
    ]
  }
}
